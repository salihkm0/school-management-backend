<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>MID DAY MEAL SCHEME - Beneficiary List</title>

<style>
@page {
    size: A4 portrait;
    margin: 5mm;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html, body {
    margin: 0;
    padding: 0;
    width: 210mm;
    font-family: "Times New Roman", serif;
    background: #fff;
    -webkit-print-color-adjust: exact;
    print-color-adjust: exact;
}

.page-container {
    width: 200mm;
    min-height: 277mm;
    margin: 0 auto 5mm auto;
    padding: 10mm 8mm;
    border: 2px solid #000;
    position: relative;
    page-break-after: always;
    box-sizing: border-box;
    background: #fff;
    display: flex;
    flex-direction: column;
}

.page-container:last-child {
    page-break-after: auto;
    margin-bottom: 0;
}

/* Logo Watermark */
.logo-watermark {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 140mm;
    height: 140mm;
    opacity: 0.04;
    z-index: 1;
    pointer-events: none;
}

.logo-watermark img {
    width: 100%;
    height: 100%;
    object-fit: contain;
}

.content-layer {
    position: relative;
    z-index: 2;
    height: 100%;
    display: flex;
    flex-direction: column;
    flex: 1;
}

/* Header */
.header {
    margin-bottom: 8px;
    text-align: center;
}

.school-logo {
    width: 25mm;
    height: 25mm;
    margin: 0 auto 5px;
}

.school-logo img {
    width: 100%;
    height: 100%;
    object-fit: contain;
}

.school-name {
    font-size: 14px;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 3px;
}

.arabic-text {
    font-size: 12px;
    font-weight: bold;
    margin-bottom: 5px;
    color: #333;
}

.header-title {
    text-align: center;
    font-size: 16px;
    font-weight: 900;
    text-transform: uppercase;
    margin-bottom: 8px;
    padding-bottom: 5px;
    border-bottom: 2px solid #000;
}

.header-info {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 5px;
    font-size: 10px;
}

.header-info td {
    border: 1.5px solid #000;
    padding: 6px 5px;
    font-weight: bold;
}

.page-number {
    float: right;
    font-size: 9px;
    margin-top: 3px;
    font-weight: bold;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #8e8e8e;
}

/* Table */
.table-container {
    margin-top: 5px;
    flex: 1;
}

table {
    width: 100%;
    border-collapse: collapse;
    font-size: 8.5px;
}

th, td {
    border: 1px solid #000;
    padding: 5px 2px;
    text-align: center;
    vertical-align: middle;
}

th {
    background: #f0f0f0;
    font-weight: 900;
    text-transform: uppercase;
    font-size: 8px;
    padding: 6px 2px;
}

td {
    background: transparent;
}

/* Main table column widths */
.main-table th:nth-child(1), .main-table td:nth-child(1) { width: 5%; }
.main-table th:nth-child(2), .main-table td:nth-child(2) { width: 8%; }
.main-table th:nth-child(3), .main-table td:nth-child(3) { width: 37%; text-align: left; padding-left: 4px; }
.main-table th:nth-child(4), .main-table td:nth-child(4) { width: 5%; }
.main-table th:nth-child(5), .main-table td:nth-child(5) { width: 10%; }
.main-table th:nth-child(6), .main-table td:nth-child(6) { width: 5%; }

/* Bottom Section - Fixed to bottom */
.bottom-section {
    margin-top: auto;
    padding-top: 10px;
}

/* Summary Table */
.summary-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 8px;
}

.summary-table th,
.summary-table td {
    border: 1.5px solid #000;
    padding: 4px 2px;
    text-align: center;
    vertical-align: middle;
    background: transparent;
}

.summary-table th {
    background: #f0f0f0;
    font-weight: 900;
    text-transform: uppercase;
}

/* Summary column widths */
.summary-table .label-col { width: 3%; font-weight: 900; }

.summary-table .st-b { width: 2.2%; }
.summary-table .st-g { width: 2.2%; }
.summary-table .st-t { width: 2.2%; }

.summary-table .sc-b { width: 2.2%; }
.summary-table .sc-g { width: 2.2%; }
.summary-table .sc-t { width: 2.2%; }

.summary-table .obc-b { width: 2.2%; }
.summary-table .obc-g { width: 2.2%; }
.summary-table .obc-t { width: 2.2%; }

.summary-table .gen-b { width: 2.2%; }
.summary-table .gen-g { width: 2.2%; }
.summary-table .gen-t { width: 2.2%; }

.summary-table .meal-boys { width: 4%; }
.summary-table .meal-girls { width: 4%; }
.summary-table .meal-total { width: 4%; }

/* Nutrition Table */
.nutrition-table {
    margin-top: 8px;
    width: 40%;
    border-collapse: collapse;
    font-size: 8px;
}

.nutrition-table th,
.nutrition-table td {
    border: 1.5px solid #000;
    padding: 5px 3px;
    text-align: center;
    background: transparent;
}

.nutrition-table th {
    background: #f0f0f0;
    font-weight: 900;
}

/* Signatures */
.signatures {
    margin-top: 15px;
    display: flex;
    justify-content: space-between;
}

.signature-item {
    text-align: center;
    width: 140px;
    font-size: 9px;
    font-weight: bold;
}

.signature-line {
    margin-top: 25px;
    border-top: 1px solid #000;
    width: 100%;
}

/* Footer */
.footer {
    margin-top: 8px;
    padding-top: 5px;
    border-top: 1px solid #000;
    overflow: hidden;
    font-size: 9px;
}

.total-students {
    float: left;
    font-weight: 700;
}

.generated-info {
    float: right;
}

/* Print optimization */
@media print {
    html, body {
        width: 210mm;
        margin: 0;
        padding: 0;
    }
    
    th {
        background: #f0f0f0 !important;
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
    }
    
    td {
        background: transparent !important;
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
    }
    
    .page-container {
        border: 2px solid #000;
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
        page-break-after: always;
    }
    
    .logo-watermark {
        -webkit-print-color-adjust: exact;
        print-color-adjust: exact;
    }
}
</style>
</head>

<body>

<%
const studentsPerPage = 35;
const totalPages = Math.ceil(students.length / studentsPerPage);
const displayAcademicYear = typeof academicYear !== 'undefined' ? academicYear : '2025-26';
const displaySchoolCode = typeof schoolCode !== 'undefined' ? schoolCode : '19057';
const displaySchoolName = typeof schoolName !== 'undefined' ? schoolName : 'P.P.M.H.S.S. KOTTUKKARA';
const displayClassName = typeof className !== 'undefined' ? className : '8 A';
const displaySchoolLogo = typeof schoolLogo !== 'undefined' ? schoolLogo : '';

// Calculate category counts
const categoryCounts = {
    ST: { B: 0, G: 0, T: 0 },
    SC: { B: 0, G: 0, T: 0 },
    OBC: { B: 0, G: 0, T: 0 },
    General: { B: 0, G: 0, T: 0 }
};

students.forEach(function(student) {
    let category = student.category || 'General';
    if (category === 'ST') category = 'ST';
    else if (category === 'SC') category = 'SC';
    else if (category === 'OBC') category = 'OBC';
    else category = 'General';
    
    const gender = student.gender || 'F';
    
    if (categoryCounts[category]) {
        if (gender === 'M' || gender === 'B') {
            categoryCounts[category].B++;
        } else {
            categoryCounts[category].G++;
        }
        categoryCounts[category].T = categoryCounts[category].B + categoryCounts[category].G;
    }
});

const totalBoys = students.filter(s => s.gender === 'M' || s.gender === 'B').length;
const totalGirls = students.filter(s => s.gender === 'F' || s.gender === 'G').length;
const grandTotal = students.length;

for (let page = 0; page < totalPages; page++) {
    const startIndex = page * studentsPerPage;
    const endIndex = Math.min(startIndex + studentsPerPage, students.length);
    const pageStudents = students.slice(startIndex, endIndex);
    const isLastPage = page === totalPages - 1;
    const isFirstPage = page === 0;
%>

<div class="page-container">
    
    <!-- Logo Watermark -->
    <div class="logo-watermark">
        <% if (displaySchoolLogo) { %>
            <img src="<%= displaySchoolLogo %>" alt="Watermark">
        <% } else { %>
            <div style="font-size: 40px; font-weight: bold; text-align: center; color: rgba(0,0,0,0.04);">
                <%= displaySchoolName %>
            </div>
        <% } %>
    </div>
    
    <div class="content-layer">
        
        <div class="header">
            <!-- School Logo on first page only -->
            <% if (isFirstPage && displaySchoolLogo) { %>
            <div class="school-logo">
                <img src="<%= displaySchoolLogo %>" alt="School Logo">
            </div>
            <% } %>
            <div class="school-name"><%= displaySchoolName %></div>
            <div class="header-title">MID DAY MEAL SCHEME <%= displayAcademicYear %> - BENEFICIARY LIST</div>
            
            <table class="header-info">
                <tr>
                    <td><strong>SCHOOL CODE</strong></td>
                    <td><%= displaySchoolCode %></td>
                    <td><strong>SCHOOL NAME</strong></td>
                    <td colspan="2"><%= displaySchoolName %></td>
                    <td><strong>CLASS</strong></td>
                    <td><%= displayClassName %> (<%= displayAcademicYear %>)</td>
                    <td><strong>Supply. Ntrn. Food</strong></td>
                </tr>
            </table>
        </div>
        
        <div class="table-container">
            <table class="main-table">
                <thead>
                    <tr>
                        <th>Sl no</th>
                        <th>Adm No</th>
                        <th>Name of Pupil</th>
                        <th>B/G</th>
                        <th>Category</th>
                        <th>Y/N</th>
                    </tr>
                </thead>
                <tbody>
                    <% pageStudents.forEach(function(student, index) { 
                        const slNo = startIndex + index + 1;
                        const gender = (student.gender === 'M' || student.gender === 'B') ? 'B' : 'G';
                    %>
                    <tr>
                        <td><%= slNo %></td>
                        <td><%= student.admissionNo %></td>
                        <td style="text-align: left; padding-left: 4px;"><%= student.name %></td>
                        <td><%= gender %></td>
                        <td><%= student.category || 'General' %></td>
                        <td>&nbsp;</td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
        
        <% if (isLastPage) { %>
        <!-- Bottom Section - Fixed to bottom -->
        <div class="bottom-section">
            <!-- Summary Table -->
            <table class="summary-table">
                <thead>
                    <tr>
                        <th colspan="2"></th>
                        <th colspan="3">ST</th>
                        <th colspan="3">SC</th>
                        <th colspan="3">OBC</th>
                        <th colspan="3">GENERAL</th>
                        <th colspan="3">Total Meal Student</th>
                    </tr>
                    <tr>
                        <th colspan="2"></th>
                        <th>B</th><th>G</th><th>T</th>
                        <th>B</th><th>G</th><th>T</th>
                        <th>B</th><th>G</th><th>T</th>
                        <th>B</th><th>G</th><th>T</th>
                        <th>Boys</th><th>Girls</th><th>Total</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- <tr>
                        <td class="label-col" style="font-weight: 900;" colspan="2">RS</td>
                        <td><%= categoryCounts.ST.B %></td>
                        <td><%= categoryCounts.ST.G %></td>
                        <td><%= categoryCounts.ST.T %></td>
                        <td><%= categoryCounts.SC.B %></td>
                        <td><%= categoryCounts.SC.G %></td>
                        <td><%= categoryCounts.SC.T %></td>
                        <td><%= categoryCounts.OBC.B %></td>
                        <td><%= categoryCounts.OBC.G %></td>
                        <td><%= categoryCounts.OBC.T %></td>
                        <td><%= categoryCounts.General.B %></td>
                        <td><%= categoryCounts.General.G %></td>
                        <td><%= categoryCounts.General.T %></td>
                        <td><%= totalBoys %></td>
                        <td><%= totalGirls %></td>
                        <td><%= grandTotal %></td>
                    </tr> -->
                    <tr>
                        <td class="label-col" style="font-weight: 900;" colspan="2">RS</td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <!-- <td></td> -->
                    </tr>
                    <tr>
                        <td class="label-col" style="font-weight: 900;" colspan="2">FS</td>
                        <td></td><td></td><td></td>
                        <td></td><td></td><td></td>
                        <td></td><td></td><td></td>
                        <td></td><td></td><td></td>
                        <td></td><td></td><td></td>
                    </tr>
                </tbody>
            </table>
            
            <!-- Total Nutrition Students Table -->
            <table class="nutrition-table">
                <thead>
                    <tr>
                        <th colspan="3">Total Nutrition Students</th>
                    </tr>
                    <tr>
                        <th>Boys</th>
                        <th>Girls</th>
                        <th>Grand Total</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td style="height: 20px;">&nbsp;</td>
                        <td style="height: 20px;">&nbsp;</td>
                        <td style="height: 20px;">&nbsp;</td>
                    </tr>
                    <tr>
                        <td style="height: 20px;">&nbsp;</td>
                        <td style="height: 20px;">&nbsp;</td>
                        <td style="height: 20px;">&nbsp;</td>
                    </tr>
                </tbody>
            </table>
            
            <!-- Signatures -->
            <div class="signatures">
                <div class="signature-item">
                    <div>Class Teacher</div>
                    <div class="signature-line"></div>
                </div>
                <div class="signature-item">
                    <div>Headmaster</div>
                    <div class="signature-line"></div>
                </div>
                <div class="signature-item">
                    <div>PTA President</div>
                    <div class="signature-line"></div>
                </div>
            </div>
            
            <div class="footer">
                <span class="total-students">Total Students: <%= students.length %></span>
                <span class="generated-info">Generated on: <%= new Date().toLocaleDateString('en-IN') %></span>
            </div>
        </div>
        <% } %>
        
        <div class="page-number">Page <%= page + 1 %> of <%= totalPages %></div>
    </div>
</div>

<% } %>

</body>
</html>